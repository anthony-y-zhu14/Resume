# Resume — Personal Resume Web App

A configurable resume web app built with Vite and TypeScript. The site renders resume data from simple config files and can output a printable PDF.

## Quick Overview

- **What:** A single-page resume site driven by TypeScript/TSX components and data in `configs/`.
- **Use:** Edit resume data in the config files, run the dev server, build for production, or generate a PDF.
